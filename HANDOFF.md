# Material Email handoff

## Current state

The Outbox recovery surface now preserves its existing ordered, single-attempt delivery semantics while making recovery actions explicit and reviewable. Retry, undo queued send to Drafts, and queue-ID History labels follow English, Hong Kong Cantonese, or semantic bilingual mode and the two independent humor levels; each control has a subject-specific accessible name and the retry/undo controls describe the stored factual error. Opening History clears unrelated action/date filters and applies the exact queue identifier, while undo moves the unchanged queued snapshot to Drafts without another transport attempt. Focused evidence is 2 unit/service files / 21 tests plus 1 / 1 offline real-Electron scenario covering a paused retry count and error after restart, exact History navigation, undo to Drafts, and a second restart with the remaining retry state intact. No live provider, duplicate-send ambiguity, packaged screen reader, or full display-scale claim is made. The open-issue scan found no issues in this repository or `agent-global-memory`; GitHub Projects was skipped after the authenticated CLI token reported missing `read:project`, and no Project state changed.

The renderer now has an explicit forced-colors and keyboard-focus contract for four recent desktop surfaces: Windows caption buttons, notification-centre actions, appearance presets, and the shared History/Changelog date picker. System-color borders keep borderless actions visible; read/dismissed/pressed notification state no longer depends on opacity or author color; native preset and calendar selects expose a keyboard-only wrapper ring when Chromium suppresses the select's own outline; and selected calendar endpoints use a contrasting `HighlightText` focus ring. Focused renderer coverage passes 2 / 2 cases and real-Electron Chromium forced-colors emulation passes 2 / 2 scenarios across all four groups. This is bounded renderer evidence, not native Windows High Contrast, screen-reader, packaged, or OS-wide certification. The open-issue re-scan found no issues in this repository or `agent-global-memory`; GitHub Projects was skipped after the one probe reported missing `read:project`, and no Project state changed.

The Windows title bar now exposes app-owned 48 × 48 Minimize, Maximize/Restore, and Close buttons backed by authenticated native `BrowserWindow` operations. All actions have keyboard-reachable English, playful Hong Kong Cantonese, and bilingual labels/tooltips selected by the two independent humor levels; maximize/restore stays synchronized with main-process events and uses a computed 3 px inset focus indicator. A bounded main-process `window-state.json` atomically persists normal bounds and maximized state, recentres disconnected-monitor coordinates, clamps oversized placement, preserves malformed input before replacement, and deliberately never restores minimized state. Dirty composer or PIM content converts both the app Close button and a native `BrowserWindow.close()` request into the existing accessible reviewed decision; cancellation retains the content and returns focus, while confirmation uses one-shot close approval. Focused evidence is 3 files / 23 unit/security/localization tests plus 3 / 3 real-Electron scenarios. Native screen-reader, high-contrast, physical multi-monitor/taskbar, packaged, and 100/125/150/200% display-scale proof remain open. GitHub Project tracking was probed once and skipped after the authenticated CLI token reported missing `read:project`; no Project item or field was changed.

The notification centre now assigns every stored record a localized account, mail, delivery, security, history, or app category alongside its localized severity. Read and dismissed state persist independently; dismissal marks the record read but leaves it visibly reviewable and restorable. Accessible controls name their record, and a closed structured action union can reuse only existing open-page/open-draft, account-sync, queued-operation, Outbox, and settings-history handlers. Legacy arbitrary label/command metadata is discarded during state migration. Focused evidence is 4 unit/service/schema/security files / 34 tests plus 1 / 1 real-Electron restart scenario covering English and Cantonese labels, state persistence, Open Settings navigation, and the polite non-modal toast region; service coverage also verifies retry metadata and append-only settings-restore undo. Live provider retry outcomes, stale-action scale, packaged assistive-technology output, and clearing-history undo remain open. GitHub Project tracking was skipped after `gh project list` reported the authenticated token was missing `read:project`; no Project item or field was changed.

Workspace tabs now add a bounded preset and transfer layer without widening the existing editor into a Word-depth claim. Material Violet, Quiet Slate, and High Contrast ship as bilingual built-in choices; users can save up to 24 validated named presets locally, apply them to one tab, delete them, reset the saved library without changing already-applied styles, and retain the library across restart. Version-1 JSON export/import runs through authenticated IPC and native Electron dialogs. The main process rejects network/UNC paths, links, non-regular files, non-UTF-8 text, files over 256 KiB, unknown tabs/fields/versions, invalid values, duplicate presets, and secret-shaped baggage; export returns only a basename. Import shows a reviewed replacement count before changing only tab overrides and saved presets. Focused evidence is 29 / 29 cases across four unit/service/IPC files plus 1 / 1 real-Electron workflow covering bilingual English-level-1/Cantonese-level-5 copy, restart persistence, actual desktop-dialog routing, exported-content inspection, reviewed restore, and reset. Complete every-element presets, Word-depth typography, full color translation, picker self-customization, packaged dialogs, and the native screen-reader/display-scale matrices remain open.

The renderer now routes the recent appearance editor, History and Changelog date pickers, notification centre, notification severity badges, and local toast bodies through one fallback-safe language/humor resolver. Each target surface has five factual English variants and five factual Hong Kong Cantonese variants; bilingual mode selects the two levels independently and emits semantic `lang="en"` / `lang="zh-HK"` spans. Toast facts are retained while only surrounding voice changes, and missing variants fall back to nonblank copy. Focused renderer coverage exercises active-language isolation, inverse bilingual levels, fallback behavior, severity labels, and toast facts; 1 / 1 real-Electron matrix passes all three modes plus English 1/Cantonese 5 and English 5/Cantonese 1 across the four target surfaces and a live toast. This is focused evidence, not a completed every-message, native-screen-reader, or full-scale audit.

Workspace tabs now deepen their bounded per-element appearance slice with persisted accent, background, and text overrides. Each color exposes a native continuous field plus synchronized HEX/HEX8, RGB, and HSL numeric entry; RGB/HSL changes preserve existing HEX8 alpha. Valid changes update the targeted tab and anchored editor preview through CSP-safe CSSOM properties, while text/background and accent/background readouts report the current WCAG ratios without silently rewriting the user's choice. Independent property reset, whole-tab reset, restart persistence, bilingual accessible names, and exact focus return remain intact. The context menu/editor and both advanced date pickers now use fixed collision-aware placement, CSS viewport clamps, contained scrolling, and reduced-motion delays of zero. Focused evidence is 6 / 6 pure color/state cases, the existing 1 / 1 real-Electron appearance workflow, and 2 / 2 new real-Electron responsive scenarios across 760 × 560 plus smaller effective CSS viewports. The smaller sizes are layout stress proxies, not native Windows display-scale proof. Numeric alpha outside HEX8, the remaining color spaces and gamut warnings, picker self-customization, Word-depth typography, every-element coverage, native screen-reader proof, and the full native display-scale matrix remain open.

The semantic-history section now reuses the Changelog calendar model in its own anchored, non-modal date-range picker. Typed ISO and active-locale dates, two-click ranges, last-seven-days/last-thirty-days/current-month/current-year/all-history presets, direct month/year jumps, and roving keyboard day focus update a history-only session model. Partial, malformed, impossible, and inverted input remains visible with bilingual inline feedback. Valid canonical bounds compose with the existing derived action checkboxes and independent plain-text/JavaScript-regex search for both visible rows and JSON export; invalid filters disable export. Focused evidence is 5 / 5 local-history model tests and 2 / 2 real-Electron History scenarios; packaged screen-reader and full scale certification remain open.

The Changelog now has an anchored, non-modal release-date range picker beside its existing independent plain-text/regex search. Typed ISO and Windows-locale dates, two-click calendar ranges, last-30-days/current-month/current-year/all-release presets, previous/next month controls, and direct month/year jumps all update one session-persisted date model. Partial, malformed, impossible, and inverted ranges stay visible with inline bilingual feedback. The day grid has roving focus with Arrow, Home/End, Page Up/Down, and Ctrl+Page Up/Down navigation; Escape and Done return focus to the trigger. Visible cards, clipboard copy, and Markdown export continue to share the same composed search/date selection. Focused evidence is 6 / 6 changelog model tests plus 1 / 1 real-Electron Changelog scenario; packaged screen-reader and full scale certification remain open.

Calendar and Tasks now provide a bounded local iCalendar pipeline. Strict authenticated IPC carries only a duplicate policy or bounded UID selection; the main process owns UTF-8 `.ics` dialogs and atomic local writes. Supported VEVENT/VTODO fields map into the PIM store in one transaction: skip is the safe default, same-type update is explicit, and historical/cross-type conflicts roll the whole file back. Selected/all export produces normalized CRLF iCalendar 2.0. No network, CalDAV sync, scheduling, attachments, recurrence expansion, or broad interoperability is claimed.

Material Email is an unreleased Windows-only Electron foundation. Main-process and preload services exist for IMAP/SMTP accounts, advisory autoconfiguration, queued offline changes and an outbox, attachment saving, demo data, local drafts, notifications, history, exports, native custom-editor approval, and window controls. A bounded local unified-folder slice aggregates cached Inbox, Starred, and Unread summaries across configured accounts, labels every row with its source account, reuses the mail regex builder, and preserves composite selection through deterministic refreshes. Visible cached rows now receive bounded in-memory conversation grouping from normalized subject plus cached `Message-ID`/`In-Reply-To`/`References`; grouping stops above 2,000 rows and leaves them individually accessible. It performs no all-account server pass and claims no server-complete threading or scalable mail index. MIME parsing, conservative message-HTML sanitization, and an isolated reader boundary are present. Packaged builds ignore `MATERIAL_EMAIL_DEV_URL`; unpackaged development accepts only strict HTTP loopback URLs at `127.0.0.1` or `[::1]`. Every IPC handler authenticates the current main window's `WebContents`, its top frame, and the exact trusted renderer URL/path. The renderer now contains Mail, Contacts, Mailing Lists, Calendar, Tasks, PIM Transaction History, Settings, Changelog, History, Notifications, and Tools/About panels plus first-run setup, non-modal compose, and structured local-organizer editors. History now has an independently searchable whole-workspace revision list, bounded parent diff previews, local Git-note labels, reviewed whole-state restore, persisted 30–3,650-day retention with exact dry-run preview, and a read-only deletion-evidence report. Apply prunes only app-owned eligible revisions after stale-head and clean-lineage checks, preserves the current tree and labels, and appends a semantic event. Inspection reports active/labeled/reflog-only revisions and Git object inventory while explicitly recording that it performs no cryptographic erasure, reflog expiry, garbage collection, backup audit, or media audit. Object reclamation, crash-injection coverage, cryptographic-erasure design, and every-record restore are not complete. Workspace tabs have a focused per-element appearance slice: validated local overrides for color, type size/weight, and radius; context-menu and direct keyboard entry; independent property reset; restart persistence; and exact focus return. Their primary tab strip also keeps focus on the selected tab during Arrow/Home/End navigation, exposes one roving tab stop with reciprocal tab/tabpanel IDs, and renders a tested inset keyboard focus indicator. This does not claim the broader Word-depth every-element editor, native screen-reader certification, or the full display-scale matrix. Packaged visual/accessibility proof remains incomplete.

Valid mail queries now build an ephemeral main-process index over at most 2,000 coherent cached summaries and 4,096 already-cached body characters per row. Plain and regex modes share the builder validator, results cap at 200, and each hit carries account, folder, conversation, matched-field, and snippet attribution. This is query-time local work, not SQLite, persistent full-text search, background indexing, server search, or large-store proof.

Message cryptography now has a bounded metadata-only trust-state foundation. A strict version-1 account profile accepts at most 16 public OpenPGP/S/MIME identity records and rejects unknown fields, plaintext secrets, private material, secret references, and verified claims. Message detail parsing inspects only the bounded top-level MIME header and records `unsigned`, `unverified`, or `unsupported` plus literal facts that signature verification and content decryption were not performed. The reader and compose surfaces expose accessible English/Cantonese/bilingual indicators; compose always says the current SMTP path sends unsigned and offers no sign/encrypt action. Focused evidence is 3 unit/parser/persistence files / 29 tests and 1 / 1 real-Electron scenario. No signature, encryption, decryption, verification, key/certificate import, audited crypto library, or provider interoperability exists.

Account setup now runs one shared deterministic preflight over both IMAP and SMTP settings before the mail service can open a transport. It rejects non-host syntax and certificate wildcard patterns, conventional implicit-TLS/STARTTLS port inversions, and invalid ports; IP literals, private/single-label names, custom secure ports, and explicit plain transport receive factual advisories. The bilingual live region marks blocking controls with `aria-invalid`, switches to an assertive alert on submit, focuses the first invalid field, and states that no server was contacted. The main process repeats blocking checks before connection, encryption, or persistence. Focused evidence is 2 unit files / 8 tests and 1 / 1 real-Electron scenario. No server certificate was inspected: certificate chain/trust/expiry/revocation/interception, an observed hostname mismatch, live provider/proxy/IPv6 behavior, and interoperability remain unproved.

An optional incoming/outgoing action can now inspect a live certificate only after the user presses it. The credential-free IPC contains endpoint, host, port, and security mode only. Direct TLS or fixed IMAP/SMTP STARTTLS negotiation shares one five-second deadline; response bytes cap at 16 KiB and displayed chains at eight certificates. Renderer output includes runtime trust, hostname match, protocol/cipher, validity, key type/size, completeness, and short one-way IDs while raw names, SANs, serials, full fingerprints, certificate bytes, and greetings remain undisplayed and unpersisted. Plain mode opens no socket. Focused evidence is 2 unit/IPC files / 15 tests and 1 / 1 real-Electron loopback scenario proving zero connections before the explicit click. This does not prove provider login or interoperability, revocation/CT status, interception, proxy behavior, or IPv6 connectivity.

OAuth account setup now has an ephemeral authorization-code/PKCE foundation instead of a pasted-token field. A configured attempt generates fresh 64-byte verifier and 32-byte state entropy, binds an exclusive ephemeral `127.0.0.1` listener, opens the authorization URL only from the main process, and accepts only the exact GET peer/host/port/path/state with one bounded code or provider error. Renderer IPC exposes status only. Authorization receipt, cancel, five-minute timeout, provider denial/error, browser/listener failure, and repeated invalid callbacks close the listener and zero/clear temporary state. The code is discarded; there is no token exchange, token persistence/logging, provider registration in production, or connected OAuth account. The main service rejects OAuth-labeled test/add requests before mail transport or persistence. Focused evidence is 8 OAuth unit tests, 29 / 29 combined IPC/security assertions, and 1 / 1 real-Electron scenario covering bilingual semantics, no token-entry path, waiting/cancel status, polling focus, and a 760 × 560 viewport. No live provider authorization or token behavior is claimed.

A separate `oauth-token-vault.ts` now provides the production main-process Windows storage boundary. Initial save and rotation require an explicit bounded provider registration; the shipped registration and revoker lists are empty. Access and refresh values are independently protected by Electron `safeStorage`, replacement uses a synchronized same-directory temporary file, rotation increments a bounded metadata generation and preserves the existing refresh ciphertext when a provider omits a new value. Provider-level clear removes active local ciphertext without claiming secure filesystem erasure. An injected registered revoker can receive decrypted tokens only inside the main process, and local ciphertext is still cleared when that call fails. Renderer IPC exposes only protection/failure state, provider registration, count, generation, expiry, and action availability. Five focused vault unit tests run with a test-only provider source; the real-Electron Settings scenario verifies bilingual accessible status, disabled production actions, no token input/key, and the 760 × 560 bound. No live exchange, provider revocation, connected account, or interoperability is claimed.

`mock-oauth-token-lifecycle.ts` continues to model exchange, expiry, refresh rotation, and revocation for tests and explicitly constructed demo harnesses only. It is not imported by production main, preload, IPC, `AppService`, or the visible demo workspace. Its ephemeral vault stores AES-256-GCM ciphertext in memory, authenticates each record against its storage key, clears replaced/deleted blobs, and zeros its process-local key on disposal. Six focused tests cover ciphertext inspection, exchange/expiry/refresh/revoke, revocation-failure deletion, invalid input/transitions, and explicit demo construction. These mock values are not provider credentials or token-lifecycle interoperability evidence.

Account setup now exposes a bounded POP3 foundation beside the existing IMAP path. Strict options cover local-demo versus explicitly unsupported live-network mode, new-only retrieval, required leave-on-server, and a `1..50` message limit; local diagnostics use conventional ports `995`/`110`. The bilingual panel disables live account and credential controls, then runs deterministic fixtures through `idle → connecting → authorization → transaction → update → disconnected`. Main-process test/add also refuse POP3 before mail transport, encryption, or persistence. Focused evidence is 5 files / 33 tests plus 1 / 1 real-Electron scenario. No live server, provider capability, login, TLS handshake, durable UIDL state, polling, retention, `DELE`, or full synchronization is claimed.

The command palette now uses the same independent plain-text-first search model and adjacent bounded JavaScript-regex builder as the audited Settings, semantic and whole-workspace History, Notifications, and Tab Manager fields. It matches the visible English and Cantonese command labels, exposes invalid regex state, returns no executable result for invalid or risky patterns, and limits <kbd>Enter</kbd> activation to its search input. <kbd>Escape</kbd> closes the nested builder before dismissing the palette. This focused slice does not close the broader every-surface, Unicode, multiline, adversarial-timeout, assistive-technology, or display-scale matrices.

Raw MIME now passes through one main-process safety policy before detail or attachment parsing. A new parse with a known server size above 32 MiB is refused before the mail service runs; IMAP source requests cap the returned range at 32 MiB plus one detection byte. The parser also rejects header blocks above 64 KiB, physical header lines above 8 KiB, header NUL bytes, decoded text above 2 MiB, decoded HTML above 4 MiB, more than 100 attachments, one decoded attachment above 20 MiB, or decoded attachments above 24 MiB combined. CID links are kept as links and synthesized text-to-HTML output is disabled to avoid needless in-memory amplification. Stable failures omit parser internals, leave the cache and message unchanged, and flow through the existing non-modal reader error with safe retry guidance. This is a local bounded-parser slice, not live-provider interoperability, a comprehensive malformed corpus, a parser hard timeout, or antivirus analysis.

A PIM service under `src/main/pim/` covers contacts, mailing lists, bounded vCard import/export, a local Home calendar, events, tasks, append-only transactions, generation recovery, cross-instance/process locking, and stale-refresh protection. It is integrated through typed preload/IPC operations and Material renderer pages. A separate provider foundation validates CardDAV/CalDAV HTTPS profiles and absolute local Windows ICS file URLs, rejects URL credentials/query/fragment data, records auth mode without accepting a secret, follows a deterministic `idle → validating → capability-review → ready` or `rejected` trace, and reports only bounded local vCard/iCalendar envelope capabilities. Interchange inspection caps input at 1 MiB / 20,000 lines / 8,192 bytes per line, requires balanced vCard 3/4 or iCalendar 2.0 components, refuses scheduling `METHOD` and attachments, and counts recurrence rules without expanding them. No DNS, socket, TLS, provider discovery/login, credential storage, read/write, ETag, sync-token, scheduling, file watching, persistence, live synchronization, or provider interoperability is claimed.

Mail mutation handling now treats false IMAP results as failures, refuses unsafe copy/delete fallback on servers without MOVE, adopts the server-returned destination UID and UIDVALIDITY without recycling the source UID, and waits for refresh when an offline or unmapped destination cannot be updated authoritatively. A permanent all-recipient SMTP 5xx returns the exact rejected recipients and keeps the draft rather than entering an outbox retry. Removing an account purges its live cache, drafts, outbox entries, and pending operations while append-only history remains available.

External message links now use a deny-by-default review flow. Electron denies every popup, the main process keeps only a short-lived single-use opaque request, and the trusted renderer shows a bilingual confirmation with the normalized URL, hostname, risk, and factual warning reasons. Confirmation revalidates expiry and HTTP(S) protocol before `shell.openExternal`; cancellation, expiry, duplicate use, and browser-launch failures do not open the link.

Remote HTML images now use a separate default-deny, per-message consent flow. The main-process sanitizer keeps the ordinary reader HTML image-free and builds a second bounded variant containing only normalized absolute HTTP(S) images plus their origin/host/protocol summary. The reader lists those exact origins in accessible English, Hong Kong Cantonese, or bilingual copy before loading anything. Consent is persisted on the cached message, survives restart, can be revoked, and resets when a new mailbox generation replaces the cached detail. The application CSP is unchanged; a consented opaque message frame adds only those origins to `img-src` while scripts, forms, frames, objects, media, connections, referrers, and same-origin access stay denied. HTTP receives a transport warning, not a certificate diagnostic.

Caution and dangerous received attachments now enter local quarantine before any user-selected save destination. The main process stores bytes under randomized `.quarantine` names and persists original metadata, source UID/UIDVALIDITY provenance, risk reasons, byte size, timestamp, and SHA-256 integrity. The Tools panel provides keyboard-accessible English, playful Hong Kong-style Cantonese, and bilingual Release/Delete decisions. Release rechecks size/hash and uses a native destination dialog; delete never releases a copy. Ordinary batch members still use the native folder chooser. This is not antivirus scanning, and no malware-clean verdict is produced.

Compose and PIM editors use saved/loaded dirty baselines. Their discard decision is accessible and restores focus, replacement attempts are guarded, and the unload guard covers either dirty editor so a whole-window close cannot silently discard the form. Send/Save operations are mutually exclusive; edits made during an in-flight operation remain visible and unsaved, while the main process preserves newer same-ID draft versions. PIM saves are bound to their originating editor and keep a retryable, factual state if post-save refresh fails. Mail account/folder/message requests use monotonic ownership so late results cannot overwrite the current view.

No clean-machine/default Windows-profile installation proof exists at this handoff. Published installers exist, and the local verifier now requires a candidate packaged launch from an absent isolated profile with exact default settings/window state, then deterministic retained settings/window evidence through candidate launch, optional strict prior-release upgrade, and uninstall. The report and workflow are guarded against calling that an interactive first launch, clean machine, default Windows profile, or Authenticode check. Hosted proof belongs to the exact GitHub Actions run and is not inferred from these source changes.

## Verification evidence

The consolidated current-tree gates pass locally. This evidence covers the source tree on this workstation; it does not certify a clean machine, hosted CI, a release, or public-provider interoperability.

| Check | Result |
| --- | --- |
| Focused regression coverage | Process/IPC trust, mail mutation and SMTP outcomes, account cleanup, renderer dirty/load state, bilingual semantics, and keyboard focus |
| Focused Windows window controls | Passed: 3 focused files / 23 tests plus 3 / 3 real-Electron scenarios for bounded persistence, off-screen recovery, corrupt-primary preservation, accessible English/Cantonese labels, sequential Tab focus, computed 3 px inset focus, minimize routing, maximize/restore synchronization, full-restart placement, and reviewed native dirty close |
| `npm run check` | Passed: typecheck; 56 files / 308 tests; 10 bundled-image checks; 30 detailed site articles/source-policy checks; production build |
| Focused changelog calendar | Passed: 6 / 6 model tests and 1 / 1 real-Electron scenario for partial/locale input, month/year navigation, cross-month selection, keyboard focus, focus return, and composed text/date filtering |
| Focused connection preflight | Passed: 2 unit files / 8 tests plus 1 / 1 real-Electron scenario covering local host/certificate-identity advisories, TLS/port conflicts, main-process no-network refusal, bilingual copy, ARIA error state, and first-invalid focus |
| Focused live TLS inspection | Passed: 2 unit/IPC files / 15 tests plus 1 / 1 real-Electron loopback scenario covering explicit-action-only network use, direct TLS, IMAP/SMTP STARTTLS without authentication, five-second total bound, redaction, mismatch reporting, and empty credentials |
| Focused OAuth foundation | Passed: 8 state-machine/PKCE/callback unit tests; 29 / 29 combined IPC/security assertions; 1 / 1 real-Electron scenario for bilingual status, no pasted-token input, disabled account actions, waiting/cancel, focus continuity, and narrow width. No provider registration, token exchange, connected account, or live-provider result is claimed. |
| Windows OAuth token vault | Passed: 6 / 6 adapter tests inside a 5-file / 36-test OAuth/IPC slice for registration gating, unavailable protection, ciphertext-only persistence, atomic access/refresh generations, direct orphaned-record clear, successful revoke-and-clear, failed-revoke local clear, and renderer-safe results. Real-Electron Settings coverage is part of the 2-scenario OAuth file and verifies bilingual status, disabled unregistered actions, absent token fields/bridge keys, and narrow width. No live provider behavior is claimed. |
| Mock-only OAuth token lifecycle | Passed: 6 / 6 focused unit tests for AES-GCM ciphertext, exchange, expiry, refresh rotation, revoke and revoke-failure local deletion, invalid inputs/transitions, and explicit demo construction. No production import or provider behavior is claimed. |
| Focused POP3 foundation | Passed: 5 files / 33 tests plus 1 / 1 real-Electron scenario for strict options, deterministic transitions, stable UIDL fixtures, pre-transport refusal, bilingual copy, disabled live controls, ARIA status, and narrow width. No provider/deletion/synchronization proof is claimed. |
| Focused PIM provider foundation | Passed: 1 unit file / 8 tests plus 1 / 1 real-Electron scenario for HTTPS/auth-mode/local-file validation, deterministic capability/state facts, bounded vCard/iCalendar envelopes, recurrence metadata without expansion, scheduling/attachment refusal, bilingual Settings semantics, absent credential input, ARIA live status, and 760 × 560 width. No live provider, credentials, sync, or interoperability proof is claimed. |
| Local iCalendar import/export | Passed: focused codec/service/provider/IPC tests inside the 51-file / 268-test consolidated gate; 1 / 1 real-Electron scenario verifies bilingual Calendar/Tasks controls, duplicate-policy persistence, selected/all enablement, and 760 × 560 overflow. No native-dialog automation, live provider, scheduling, recurrence expansion, or broad interoperability proof is claimed. |
| Focused MIME safety | Passed: 4 files / 33 tests covering bounded IMAP ranges, known-size preflight, raw/header/text/HTML ceilings, NUL headers, attachment count/bytes, stable errors, cache preservation, unterminated multipart, cryptography-state propagation, and unchanged sanitization |
| Message cryptography trust states | Passed: 3 focused unit/parser/persistence files / 29 tests plus 1 / 1 real-Electron scenario covering unsigned/unverified/unsupported states, bilingual reader/compose copy, absent sign/encrypt actions, strict identity metadata, and rejected plaintext secret/private-key fields. No cryptographic operation or interoperability is claimed. |
| Local NSIS lifecycle | Focused contract coverage passes 7 / 7 for strict versions/arguments/PE structure, deterministic default/retained profile fixtures, exact isolated smoke validation, and false stronger-claim fields. Earlier published `0.19.1` → disposable `0.999.1` artifact evidence remains historical; the current exact installer must be rebuilt and exercised. |
| Focused external-link tests | Passed: 4 existing safety tests plus 5 queue tests and IPC validation; 22 tests across 4 files |
| Focused remote-content tests | Passed: sanitizer/default-deny, strict IPC, cache migration, persisted allow/revoke, reader CSP, focus, restart, and bilingual semantics; real Electron consent and bilingual scenarios passed 2 / 2 |
| Focused tab-appearance tests | Passed: 6 pure normalization/color/contrast/reset cases plus 1 real-Electron workflow covering context-menu/direct keyboard access, HEX/RGB/HSL editing, live target/preview styling, contrast status, persistence, both reset scopes, and focus return |
| Focused appearance preset/theme transfer | Passed: 29 / 29 cases across four unit/service/IPC files plus 1 / 1 real-Electron workflow covering named preset apply/save/restart/reset, bilingual independent tone, native-dialog routing, strict safe JSON content, reviewed import, and restored tab styling |
| Focused responsive/accessibility tests | Passed: 2 / 2 real-Electron scenarios covering CSP-safe computed tab styles, editor and History/Changelog picker collision containment, horizontal overflow, focus preservation/return, reachable completion controls, and reduced-motion delay suppression at 760 × 560 plus 608 × 448, 507 × 373, and 380 × 280 effective CSS viewports. The smaller sizes are not native Windows display-scale certification. |
| Focused language/humor matrix | Passed: renderer model coverage for five-level selection, inverse bilingual levels, nonblank fallback, factual toast preservation, and localized severity labels; 1 / 1 real-Electron scenario across appearance, History, Changelog, Notifications, and a live bilingual toast. |
| Focused workspace-tab accessibility | Passed: 1 / 1 real-Electron scenario covering ArrowRight/Home/End focus retention, one roving tab stop, reciprocal tab/tabpanel semantics, and the computed 3 px inset focus indicator |
| Focused command-palette search | Passed: 8 / 8 matcher and command-filter assertions plus 1 / 1 real-Electron scenario covering literal default search, physical builder anchoring, invalid execution guards, nested Escape, regex narrowing, and Enter activation |
| Focused local-history tests | Passed: 5 / 5 renderer model tests plus 2 / 2 real-Electron History scenarios covering labels, parent diff, redaction, retention, deletion evidence, typed date feedback, month/year and keyboard calendar navigation, named presets, range selection, focus return, and composed date/action/regex filtering |
| Focused history retention | Passed: 29 / 29 focused repository/model/IPC/persistence/security tests; 1 / 1 real-Electron history workflow covers persisted retention selection and dry-run controls. Unit coverage proves exact candidates, stale-preview refusal, non-app-owned blocking, and current-tree/label preservation. |
| Focused deletion evidence | Passed: 18 / 18 repository/model/security tests and 1 / 1 real-Electron History workflow. Post-prune unit evidence reports reflog-only revisions and literal false values for every unsupported erasure or maintenance guarantee. |
| Real-Electron coverage | Previously recorded full suite: 15 / 15; focused responsive/accessibility scenarios: 2 / 2; focused tab-appearance scenario after computed-style correction: 1 / 1; focused remote-content consent/restart and bilingual scenarios: 2 / 2; focused workspace-tab accessibility scenario: 1 / 1; focused command-palette search scenario: 1 / 1; focused local connection preflight scenario: 1 / 1; focused explicit live TLS inspection scenario: 1 / 1. The expanded full suite was not rerun in this pass. |
| Clean-machine and assistive-technology matrices | Not completed |

The coverage targets JSON/persistence behavior, process and IPC trust boundaries, bounded MIME/HTML behavior, regex behavior, exact mail mutation and recipient outcomes, account cleanup, PIM save/load state, bilingual semantics, and discard handling. Public-provider interoperability, a broad malformed-message corpus, parser wall-time isolation, clean-machine packaged behavior, the full screen-reader/scaling matrix, remote PIM synchronization, and release delivery remain unproved.

## Security facts

- Renderer sandboxing, context isolation, disabled Node integration, navigation denial, and permission denial are configured.
- Message popups are denied; external HTTP(S) links require a trusted-renderer confirmation request with bounded opaque IDs, 60-second expiry, single-use consumption, and protocol revalidation before browser launch.
- Packaged renderer startup ignores the development URL environment variable; unpackaged development permits only exact HTTP loopback hosts.
- Every IPC operation authenticates the current main `WebContents`, top frame, and exact trusted renderer URL/path before validating its bounded payload.
- Account secrets are encrypted through Electron `safeStorage` before JSON persistence and omitted from public account summaries.
- New OAuth attempts keep URL/state/verifier/code in ephemeral main-process memory, expose only bounded status IPC, zero/clear attempt buffers on every terminal path, call no token endpoint, and write nothing to account state, history, notifications, or logs. Production provider registration is empty and OAuth-labeled account add/test requests are refused.
- The Windows OAuth vault requires an explicit provider registration before token writes, uses `safeStorage` only in the main process, writes ciphertext plus bounded metadata atomically, exposes no token/ciphertext/account/scope through IPC, and permits local clear independently from provider availability. Production provider/revoker lists remain empty.
- The mock token lifecycle is isolated test/demo code with no filesystem, network, IPC, or logging route. Its ephemeral AES-GCM vault is not durable Windows protection and is not imported by the production vault.
- Message HTML is allowlisted. Its default document removes images; its separate consent variant retains only normalized HTTP(S) images and loads exact listed origins after a persisted per-message decision.
- Raw MIME reads and decoded parser output have fixed local safety ceilings; refusal leaves message/cache state unchanged and does not expose parser internals.
- OpenPGP/S/MIME handling is metadata-only: bounded top-level MIME labels can produce unsigned, unverified, or unsupported states, but no signature or sender is verified and no content is decrypted. Identity profiles reject keys, passphrases, secret references, and verified claims before persistence.
- State writes use a same-directory temporary file and rename and serialize concurrent updates.

Open security work includes live-provider certificate revocation/CT/interception and interoperability matrices, broader IPC payload testing, live-server remote-image failure coverage, antivirus/content scanning and reputation integration, OAuth provider registration/code exchange/provider refresh and revocation/scope review/account authentication, vault migration/recovery testing, audited cryptographic messaging and key lifecycle, and PIM at-rest encryption decisions. The bounded message-cryptography trust-state metadata, OAuth PKCE/loopback state machine, Windows vault adapter, local connection preflight, explicit redacted live inspection, and safe-link review are implemented; none is cryptographic or provider interoperability evidence.

## Documentation delivered in this pass

- Compact root README and public contributor rules
- Categorized per-feature documentation with explicit status and gaps
- LibreOffice-only source-reference policy pinned to inspected official desktop-behavior paths
- Self-contained local Material Design 3 documentation site
- Roadmap, handoff, and MPL-2.0 license

## Next verification sequence

1. Repeat `npm run check` and the expanded Electron suite in hosted CI against the exact committed tree.
2. Exercise demo account, settings, every search/regex surface, history, notifications, compose, local-organizer pages, attachment saving, and export using full keyboard-only and screen-reader workflows.
3. Test real IMAP/SMTP behavior through secure credential intake without recording private data.
4. Expand the focused MIME safety cases into broad malformed-message, parser wall-time/isolation, attachment-content, regex, IPC, and persistence matrices.
5. Let the hosted prior-release gate exercise the exact candidate's absent isolated-profile defaults and retained settings/window state, then separately repeat default-profile install, interactive first launch, upgrade, uninstall, retained-data, and Authenticode checks on a clean disposable Windows environment.
6. Verify CI/release automation, then publish only after the exact run and artifact are verified.
7. Deploy the documentation site, verify its base path and assets, and only then set the repository homepage.

## Known open product gaps

Live-provider OAuth registration and connected accounts; live POP3 transport/provider login/TLS, durable UIDL tracking, polling, deletion/retention semantics, and synchronization; server-complete all-account synchronization/threading; SQLite mail indexing; cryptographic history erasure; remote PIM; message cryptography; antivirus; clean-machine lifecycle proof; hosted verification; wiki synchronization; and Pages deployment remain open. The local-only POP3 state machine is foundation evidence, not a live-mail claim.
